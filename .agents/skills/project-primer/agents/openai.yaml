interface:
  display_name: "Project Primer"
  short_description: "Routes to local references for stored text, Markdown, docs, Fish, Orchard, APM, and commits"
  default_prompt: "Use $project-primer before exploring or changing this repository."
