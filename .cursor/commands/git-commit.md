---
name: Git: commit
description: Conventional commit per repo rules and the git-commit skill workflow.
---

# Git: commit

1. **Follow `.cursor/rules/git-commit.mdc`** for message format and capitalization.
2. **Follow the `git-commit` skill** (`git-commit` / Git Commit with Conventional Commits): analyze diff, stage as needed, then commit. Do not skip hooks unless the user asks.
