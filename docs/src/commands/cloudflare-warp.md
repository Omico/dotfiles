# Cloudflare WARP

**Platforms:** macOS (`darwin`)

## `setup-cloudflare-warp`

Install the Cloudflare WARP cask, register the client, connect, set `warp+doh` mode, and add Tailscale DERP hostnames and IP routes for split tunneling.
